{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: gcc build active file",
            "command": "cd ${workspaceRoot} && make re",
            "options": {
                "cwd": "/usr/bin"
            }
        },
        {
            "type": "shell",
            "label": "valgrind",
            "command": "cd ${workspaceRoot} && make re && valgrind --leak-check=full --track-origins=yes ./minishell",
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": []
        },
        {
            "type": "shell",
            "label": "test",
            "command": "cd ${workspaceRoot} && make re && cd tester && rm minishel || true && bash test.sh --fast",
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}
